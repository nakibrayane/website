<%*
const date = tp.date.now("YYYY-MM-DDTHH-mm-ssZZ");
const content = `---
draft: true
title: ${tp.date.now("YYYY-MM-DDTHH-mm-ssZ")}
date: ${tp.date.now("YYYY-MM-DDTHH-mm-ssZ")}
---`;

await tp.file.create_new(content, "index.ar", true, `microblog/${date}`);
await tp.file.create_new(content, "index.en", false, `microblog/${date}`);
%>
