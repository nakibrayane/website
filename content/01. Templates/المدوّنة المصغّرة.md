<%*
const fileName = tp.date.now("YYYY-MM-DDTHH-mm-ssZZ");
const date = tp.date.now("YYYY-MM-DDTHH:mm:ssZ");
const content = `---
draft: true
title: ${date}
date: ${date}
---`;

await tp.file.create_new(content, "index.ar", true, `microblog/${fileName}`);
await tp.file.create_new(content, "index.en", false, `microblog/${fileName}`);
%>
